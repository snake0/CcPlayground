//
// Created by SNAKE on 2019-09-09.
//

#include "Vector.h"
#include <iostream>
#include <vector>

using namespace std;

template<typename Object>
void printVector(const Vector<Object> &v) {
  for (auto &x:v)
    std::cout << x << ' ';
  std::cout << '\n';
}


int main() {
//  Vector<int> v(2);
//
//  v[0] = 2;
//  v[1] = 4;
//  printVector(v);
//  v.push_back(3);
//  printVector(v);
//  Vector<int> s(4,8);
//  s.swap(v);
//  printVector(v);
//  printVector(s);

}